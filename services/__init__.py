"""Reusable domain services (parser, embeddings, graph).

These packages are intentionally decoupled from the web layer so they can be
imported by the backend, background workers, or CLI tooling alike.
"""
