# Frontend Source

Root of the React application source tree.

## Entry Point

`main.tsx` (to be implemented) will:
1. Create React root
2. Wrap app in QueryClientProvider and RouterProvider
3. Import global Tailwind styles from `index.css`

## Path Aliases

`@/` maps to `src/` via Vite and TypeScript config.

## Subdirectories

See README in each subdirectory for detailed purpose and conventions.
