# Public Assets

Static assets served directly by Vite without processing.

## Planned Contents

| File           | Purpose                    |
|----------------|----------------------------|
| `favicon.ico`  | Browser tab icon           |
| `logo.svg`     | Application logo           |
| `robots.txt`   | Search engine directives   |

## Notes

Files here are copied as-is to the build output root (`dist/`).
Reference in HTML via absolute paths: `/favicon.ico`
